{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the GitBound main window receives core application access.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
